# 변경 이력

## 2026-09-15

### record.py

- 녹음 형식을 **Ogg/Opus** 로 변경. 
- 종료 절차를 `q` → `SIGINT` → `SIGKILL` 로 변경. 파일을 깨뜨리는 SIGTERM 제거
- ffmpeg 의 stderr 를 파이프로 열어두고 읽지 않아 버퍼가 차면 멈추던 문제 수정
- 녹음이 끝나면 `ffprobe` 로 읽히는 파일인지 확인한 뒤 목록에 넣는다
- 녹음이 도중에 끊기면 버튼이 "종료" 상태로 굳어 파일을 꺼낼 수 없던 문제 수정.
- 녹음 초기 무음 탐지 로직 추가

### sumarize.py

- 저장 형식 **`.md` / `.txt`** 선택 추가. `.txt` 는 확장자만 바꾸는 게 아니라
  마크다운 없이 생성하고, 남은 표기도 걷어낸다.

### gui.py
- 저장 폴더의 이름을 지정할 수 있도록 수정 
- 고정 규칙 추가
  - 대본에 한글로 음차된 영어 용어를 원래 표기로 되돌린다
    (리인포스먼트 러닝 → reinforcement learning)
  - 영어 전문 용어는 번역하거나 음차하지 않는다
  - 분량은 공백 포함 1500~2500자
- 같은 이름의 파일이 여러 폴더에 있을 때, 요약이 엉뚱한 대본을 대상으로
  만들어지던 문제 수정


### launcher.command

- 런처가 심볼릭 링크(바탕화면 바로가기)로 실행되면 경로를 잘못 잡던 문제 수정
- 런처의 파이썬 경로 하드코딩 제거.

### 기타

- 기본 엔진을 `Whisper(local)` 로 변경.
- 시작할 때 Groq 키를 검사하고 경고창을 띄우던 문제 수정.
  이제 요약에 필요한 Gemini 키 상태를 로그로 알려주기만 하고 실행을 막지 않는다

---

## 2026-09-09

- 시스템 소리 녹음(BlackHole), 받아쓰기, 요약까지 이어지는 GUI
- 받아쓰기 엔진 3종, Gemini 요약, 요약 후 원본 정리
